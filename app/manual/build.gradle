plugins {
    id 'application'
}

dependencies {
    implementation(
            project(':ManualConfig'),
            project(':entities'),
            project(':usecase')
    )
}

application {
    mainClass.set("recipes.foodbar.manual.Main")
}